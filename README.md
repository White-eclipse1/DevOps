# Arte delulu

Frontend en React + Vite + TypeScript para el portafolio de Lulú Cárdenas.

## Desarrollo

```powershell
npm install
npm run dev
```

## Build

```powershell
npm run build
```

Cloudflare Pages debe usar:

- Build command: `npm run build`
- Build output directory: `dist`
