{
  "name": "galeria-axiom-tail",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "deploy:dev": "wrangler deploy --env dev",
    "deploy:production": "wrangler deploy --env production"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
